TICKET_ID: dev-045
TITLE: 자산지도 연결선·애니메이션 버그 즉시 그룹 — 토글 분리·마커 고정·핸들 분배 (이슈6·3·7)
SCOPE:
  - app/asset-map/asset-map-content.tsx (단일 파일)
ACCEPTANCE:
  - 애니메이션 on/off 토글 시 노드 위치·팔레트가 초기화되지 않는다 (이슈6)
  - 신규 생성 엣지가 토글 상태를 즉시 반영 (껐다 켤 필요 없음, 이슈3-A)
  - 화살표 크기가 방향(BI/UNI)·토글과 무관하게 고정 (이슈3-B)
  - 같은 노드·같은 변에서 들어오는/나가는 선이 겹쳐 숨지 않는다 (이슈7)
  - npm run build / lint 통과(기준선 동일)
COMPLEXITY: medium
DEPLOY_FLAG: false
SECURITY_FLAG: false

## 진단 (asset-map-edge-bugs.md 클러스터 A + C 일부)
- 이슈6: fetchGraph useCallback이 animateEdges 의존(2804) → 토글이 전체 재로드(dagre 재배치·팔레트 리셋) 유발
- 이슈3-A: 신규 엣지(onConnect)에 animated 누락 → 토글 우회로 의존
- 이슈3-B: markerEnd markerUnits 미지정 → 화살촉 크기가 strokeWidth(BI3/UNI2) 종속
- 이슈7: distributeEdgeHandles가 target/source 독립 그룹 → 같은 변 incoming/outgoing 동일 슬롯 공유로 겹침

## 수정
- animateEdgesRef 도입, fetchGraph 의존성에서 animateEdges 제거, 엣지 빌드는 ref로 읽기
- 토글 핸들러: fetchGraph 재실행 대신 setEdges in-place animated 갱신(위치·팔레트 무변경)
- 신규 엣지(onConnect)에 animated 추가
- markerEnd 2곳에 markerUnits: "userSpaceOnUse"(고정 px)
- distributeEdgeHandles: target/source를 ${node}:${side} 단일 네임스페이스로 합산 슬롯 배분
- _handles 주입 2곳: 빈 배열도 항상 주입(stale 핸들 정리, if 가드 제거)

## 회귀 안전
- dev-032는 workspace-id 재발화만 막음 — animateEdges/view 재발화는 미처리였고 본 수정이 정합.
- 저장(flushSave)·복원 로직 무변경 → 영구소실 영역과 무관.

## 남은 버그 (별도 PR)
- 이슈1·4 (비-all 뷰 노드 역산·가시성 화이트리스트) — 고위험, flush 가드 선행 필요
- 이슈2 (BI 양방향 표현) — 커스텀 edge 타입 신규
- 이슈5 (조건부 조건 표시) — 스키마(AssetLink.condition) 승인 필요 + PATCH DIRECTIONS 4값 통일

## 검증
- npm run build 통과(94/94), lint 기준선 동일(에러 0).
- 런타임 권장: 애니메이션 토글 → 위치 유지 / 신규 연결 즉시 애니메이션 / 겹친 선 둘 다 표시.
