TICKET_ID: dev-048
TITLE: 자산지도 버그 2차 — 네트워크 탭 표시·탭 전환 연결선 소실·BI 양방향 (이슈1·4·2)
SCOPE:
  - app/api/asset-map/route.ts — network 뷰 자산 조회 OR(이슈1)
  - app/asset-map/asset-map-content.tsx — 클라 배치·가시성 화이트리스트·BI 화살촉 (이슈1·4·2)
ACCEPTANCE:
  - network 탭에 NETWORK 링크 없는 네트워크 장비(deviceType="Network")도 표시 (이슈1)
  - 전체/PII/네트워크/데이터흐름 탭 전환 시 연결선이 사라지지 않음 (이슈4)
  - BI 양방향 엣지가 양쪽 화살촉으로 UNI와 구분됨 (이슈2)
  - npm run build / lint 통과(기준선 동일)
COMPLEXITY: medium
DEPLOY_FLAG: true   # route.ts 동작 변경
SECURITY_FLAG: false

## 수정 (asset-map-edge-bugs.md)
- 이슈1: route.ts network 뷰 자산을 "엣지 연결 OR deviceType=Network"로 + 클라 placedIds 에 Network 장비 추가
- 이슈4: visibleEdgeIds 페이지 가시성은 all 뷰 전용 — ① 렌더 필터 view==="all"만 ②
  flushSave/beforeunload 가 all 뷰에서만 visibleEdgeIds 갱신(비-all 저장이 all 가시성 손상 방지).
  flushSave/beforeunload 는 view 를 viewRef 로 읽음.
- 이슈2: BI markerStart 를 명시 화살촉 객체로(기존 context-stroke 약한 marker → 양쪽 또렷).

## 회귀 안전
- 이슈4는 dev-032 영구소실 영역: visibleEdgeIds 를 view별로 분리(all만 갱신)해 다른 뷰 저장이
  all 가시성을 덮어쓰지 않게 함. all 뷰 동작 불변.
- 남은: 이슈5(조건부, 스키마) → dev-049. BI 양방향 동시 흐름 애니메이션(커스텀 edge) → 후속.

## 검증
- npm run build 통과(94/94), lint 기준선 동일(에러 0).

## 추가 — 이슈5 (조건부 조건 표시) 동일 PR 포함
LinkModal 이 asset-map-content.tsx 내부라 #113(같은 파일)과 충돌 회피 위해 dev-048 에 합침.
- schema: AssetLink.condition String? 추가 (additive·무위험)
- PATCH DIRECTIONS ["UNI","BI"]→4값 통일 (기존 REVERSE/CONDITIONAL 수정 시 400 거부 동반 버그)
- asset-links POST/PATCH + asset-map route + AssetEdge 타입 + edgeObj.data 에 condition 통과
- LinkModal: direction==="CONDITIONAL" 일 때 조건 textarea + payload
- EdgeDetailModal: 조건부 엣지의 활성화 조건 표시
- i18n: assetMap.conditionPlaceholder 7파일
