TICKET_ID: dev-036
TITLE: 도메인/SSL — 비주기 연수 입력·한국어 주기 표기·도메인 전용 상태 어휘 (1단계)
SCOPE:
  - lib/domain-format.ts (신규) — 주기 표기·도메인 상태 라벨 단일 출처
  - app/domains/page.tsx, [id]/page.tsx — 목록 주기 병기·"3Y"→로케일 표기·상태 라벨 오버라이드
  - app/domains/new/page.tsx, [id]/edit/page.tsx — 주기 "직접 입력(개월)" + 생성 폼 상태 선택
  - app/api/assets/route.ts — POST status 수용(vEnum 검증, 미지정 시 기존 기본값)
  - lib/i18n/* — domain 키 8종(상태 5 + 주기 표기 3) 7파일
ACCEPTANCE:
  - 18개월 같은 비주기 연수를 폼에서 직접 입력 가능, edit 로딩 시 프리셋 외 값 보존
  - 주기가 "3Y" 가 아니라 "3년"/"18개월"(로케일)로 표기, 목록 비용에 주기 병기
  - 도메인 상태가 "재고" 대신 미연결/보유·운영 중·휴면·해지 예정·해지 완료로 표시
  - 생성 폼에서 상태 선택(기본 운영 중), POST 가 status 수용
  - npm run build / lint 통과
COMPLEXITY: medium
DEPLOY_FLAG: true   # POST /api/assets 동작 추가(status 수용)
SECURITY_FLAG: false

## 진단 (백로그 F4)
3년(36개월)은 이미 지원 — 실제 공백: ① 목록 주기 표시 없음 ② 프리셋 외 임의 개월 입력 불가
(+ edit 시 프리셋 외 값이 select 공백/덮어쓰기) ③ "3Y" 영문 약식 ④ 도메인이 하드웨어용
상태(IN_STOCK=재고) 사용 + 생성 시 상태 지정 경로 없음(무조건 재고로 시작).

## 2단계 (보류 — 스키마 승인 필요)
registrant(소유권자), onlineStatus+lastCheckedAt(HTTP/DNS 온라인 체크 cron).

## 검증
- npm run build 통과(94/94), lint master 기준선과 동일(신규 0).
