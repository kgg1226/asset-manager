TICKET_ID: dev-057
TITLE: 갤러리 페이지→폴더 드래그 앤 드롭
SCOPE:
  app/asset-map/asset-map-content.tsx  (카드 draggable·폴더 드롭존·루트 복귀 드롭존)
ACCEPTANCE:
  - 페이지 카드를 폴더 카드에 드롭 → PUT views/[id] {folderId} 이동, 드롭 대상 폴더 하이라이트
  - 폴더 내 페이지를 그리드 루트에 드롭 → folderId:null 루트 복귀
  - 드래그 후 click 오발화 없음(loadWorkspace 미실행), 기존 우클릭 "폴더로 이동" 메뉴 보존
COMPLEXITY: low
DEPLOY_FLAG: false
SECURITY_FLAG: false

---

## 변경
- renderPageCard: draggable + onDragStart(dragPageRef {id, folderId}) + onDragEnd(didDragRef 다음 틱 해제).
  onClick 은 didDragRef 가드 — 드롭 직후 loadWorkspace 오발화 방지.
- 폴더 카드: onDragOver(하이라이트 ring)·onDragLeave·onDrop(stopPropagation — 루트 드롭존 버블 방지,
  같은 폴더면 no-op) → 기존 movePageToFolder(PUT views/[id] {folderId}) 재사용.
- 그리드 컨테이너: 폴더 소속 페이지만 드롭 허용 → folderId:null 루트 복귀.
- 서버 무변경(views PUT folderId 기존 지원). i18n 신규 키 없음(시각 피드백은 ring 하이라이트).

## 검증
- tsc 0 · lint 0 errors/86 warnings · build 98/98
