TICKET_ID: dev-060
TITLE: 배포 버전 배지 + 버전 관리 체계 도입
SCOPE:
  package.json                       (version 0.3.0 → 1.60.0)
  next.config.ts                     (빌드 시 버전·빌드일 주입)
  app/_components/top-header.tsx     (우측 상단 배지)
  tasks/versioning.md                (규칙 문서, 신규)
ACCEPTANCE:
  - 헤더 우측 상단에 v{버전} · {빌드일} 이 작은 회색 글씨로 표기(모바일 숨김)
  - 버전은 빌드 시점 고정 — 배지 = "이 컨테이너가 어느 빌드인가"의 증거
  - 버전 체계 1.<dev티켓>.<핫픽스> 문서화, 이후 모든 PR 에서 bump
COMPLEXITY: low
DEPLOY_FLAG: false  (표기 자체는 다음 배포에 포함되어야 보임)
SECURITY_FLAG: false

---

## 배경 (사용자 요청)
"버전이 최신인지 모르겠어" — 배포본이 최신인지 확인할 수단 부재. 우측 상단 9pt 회색 표기 + 버전 관리 요청.

## 구현
- next.config.ts 가 package.json version + 빌드일을 NEXT_PUBLIC_* 로 빌드 시점 주입(런타임 아님).
- TopHeader 우측에 text-[11px] gray-400 배지: "v1.60.0 · 2026-07-03" (sm 미만 숨김, hover 로 build 날짜).
- 버전 체계: 1.<최신 dev 티켓>.<핫픽스> — 버전만 보면 반영 티켓을 알 수 있음. tasks/versioning.md.
- 운영 검증법: 화면 배지 ↔ 최신 머지 티켓 번호 비교. 불일치 = 옛 이미지.

## 검증
- tsc 0 · lint 0 errors/86 warnings · build 98/98 · 빌드 청크에 1.60.0 주입 확인
