TICKET_ID: dev-056
TITLE: 자산맵 가시성 보완 + 중복 정리 진입점 — 비-all 생성 링크 화이트리스트 편입·모든 연결 표시/숨김·dedup UI
SCOPE:
  app/asset-map/asset-map-content.tsx  (화이트리스트 편입·토글·dedup 핸들러·툴바/메뉴 항목)
  lib/i18n/** (types + 6로케일)         (5키)
ACCEPTANCE:
  - 비-all 뷰에서 연결 생성 시 all 뷰 visibleEdgeIds 에 새 링크 편입 → all 복귀 시 즉시 보임(기존 가시성·위치 불변)
  - all 뷰 툴바 + 빈 캔버스 우클릭에 "모든 연결 표시(숨김 N)/숨김" 토글 — 비-all 뷰 비노출
  - "중복 연결 정리": dedup dryRun 미리보기(N개·G그룹 confirm) → 확정 실행 → fetchGraph 반영
COMPLEXITY: medium
DEPLOY_FLAG: false
SECURITY_FLAG: false

---

## 배경
결점 진단(사용자 보고): ① 비-all 뷰에서 만든 연결이 all 화이트리스트에 영영 미편입 → 전체 뷰에서 안 보임
② 누적·중복 연결을 화면에서 정리할 진입점 부재 ③ 페이지별 가시성을 사용자가 조작할 수단 부재.

## 변경
- **B(편입)**: handleSaveLink 에서 비-all 뷰 생성 시 ws.edgeVisibility.visibleEdgeIds 에 append-only 병합
  (ref+state 동시, dev-032 규칙). 화이트리스트 없는(null=전체표시) 페이지는 불변 — 배열 신설 시 나머지가 숨는 사고 방지.
  flushSave 비-all 가드(dev-049)는 existingVis 를 그대로 전송하므로 별도 PUT 불요 — 충돌 0.
- **토글**: allLinkIdsRef(fetchGraph all-뷰 스냅샷) 기준 "모든 연결 표시(숨김 N 배지)/숨김".
  명시 배열로 결정적 설정(null 대입은 레거시 의미와 충돌). 툴바(애니메이션 토글 옆) + 빈 캔버스 메뉴.
- **dedup 진입점**: 빈 캔버스 우클릭 → dryRun 미리보기 → {n}/{g} confirm → 실행(dev-054 API).
- i18n 5키(showAllLinks/hideAllLinks/dedupLinks/dedupNone/dedupConfirm) × 6로케일.

## 검증
- tsc 0 · lint 0 errors/86 warnings(신규 0) · build 98/98
- nodePositions/sectionData·flushSave/beforeunload 가드 코드 무접촉(dev-048/049 회귀 없음)
