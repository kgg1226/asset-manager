# Ticket dev-008

TICKET_ID: dev-008
TITLE: 디자인 레퍼런스 디렉토리 정리 — docs/design/ 이전 + 중복 사본 제거 + 데모 자격증명 제거
SCOPE:
  - colors_and_type.css → docs/design/colors_and_type.css
  - preview/ → docs/design/preview/
  - ui_kits/ → docs/design/ui_kits/
  - assets/logo-placeholder.svg → docs/design/assets/logo-placeholder.svg
  - fonts/geist-mono-latin.woff2 → docs/design/fonts/geist-mono-latin.woff2
  - assets/{file,globe,next,vercel,window}.svg (삭제 — public/ 사본과 동일)
  - docs/design/ui_kits/asset-manager/index.html (스택 내부 참조 갱신 + 데모 자격증명 제거)
ACCEPTANCE:
  - 프로젝트 루트에 `assets/`, `fonts/`, `preview/`, `ui_kits/`, `colors_and_type.css` 가 모두 사라짐
  - `docs/design/` 단독으로 모든 비주얼 레퍼런스가 자기충족(self-contained) 동작
  - `docs/design/ui_kits/asset-manager/index.html` 단독 오픈 시 폰트·컬러 적용, 로그인 데모 동작
  - `admin / changeme123` 하드코딩 제거 — 임의 입력 또는 빈 입력으로 데모 진입 가능
  - npm run build 통과
COMPLEXITY: low
DEPLOY_FLAG: false
SECURITY_FLAG: false (UI 데모 정적 파일, 실제 인증과 분리)

## 배경
PR #86 (dev-007) 으로 도입한 디자인 레퍼런스가 프로젝트 루트에 평탄하게 흩어져 있고
일부 파일은 `public/` 사본과 중복. 정리하여 표준 위치(`docs/design/`)로 이전하고
참조 경로를 갱신.

## 비-수정 결정
- `public/fonts/geist-mono-latin.woff2` 는 그대로 유지 (Next.js 앱이 사용)
- 디자인 킷 자기충족을 위해 `docs/design/fonts/geist-mono-latin.woff2` 에 사본 1개 유지
  (런타임에 영향 없는 정적 디자인 자료이므로 중복 30KB 허용)

## 경로 변경 매트릭스
| 이전 위치 (root) | 새 위치 |
|---|---|
| `colors_and_type.css` | `docs/design/colors_and_type.css` |
| `preview/*.html` | `docs/design/preview/*.html` |
| `ui_kits/asset-manager/*` | `docs/design/ui_kits/asset-manager/*` |
| `assets/logo-placeholder.svg` | `docs/design/assets/logo-placeholder.svg` |
| `fonts/geist-mono-latin.woff2` | `docs/design/fonts/geist-mono-latin.woff2` |
| `assets/{file,globe,next,vercel,window}.svg` | (삭제 — `public/` 동일 사본 보존) |

## 참조 갱신
- `docs/design/ui_kits/asset-manager/index.html`
  - `<link rel="stylesheet" href="../../colors_and_type.css">` → `../../colors_and_type.css` (변경 없음, 상대 위치 동일)
  - `assets/logo-placeholder.svg` 참조가 있다면 동일 디렉토리 트리 유지로 무변경
- `colors_and_type.css` 의 `url("fonts/geist-mono-latin.woff2")` — 동일 트리(`docs/design/fonts/`) 유지로 무변경
