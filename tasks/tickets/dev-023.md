TICKET_ID: dev-023
TITLE: 코드 리뷰 후속 — React Hook 크래시/불순 렌더 수정 + SUPER_ADMIN 권한 가드(Phase 2 일부)
SCOPE:
  - app/settings/notifications/page.tsx
  - app/_components/data-table.tsx
  - app/licenses/licenses-content.tsx
  - app/admin/actions.ts
  - app/api/admin/users/route.ts, app/api/admin/users/[id]/route.ts
  - app/api/admin/gdrive-config/route.ts, app/api/admin/feature-flags/route.ts
ACCEPTANCE:
  - lint 에러 21 → 18 (rules-of-hooks 1, impure-render 2 해소), 신규 에러 0
  - npm run build 통과
  - 비-SUPER_ADMIN ADMIN 은 role 변경/ADMIN 생성/시스템설정 변경/ SUPER_ADMIN 강등·삭제 불가(403/에러)
COMPLEXITY: medium
DEPLOY_FLAG: false
SECURITY_FLAG: true

## 배경
git clean 상태에서 dev-022(SUPER_ADMIN) 직후 코드 리뷰 진행. 4개 발견:
- #1 (심각) settings/notifications/page.tsx: 조기 return 뒤 useState 호출 → Hook 순서 변동 런타임 크래시
- #2 (중간) data-table.tsx: Math.random() key → 매 렌더 행 리마운트(포커스/상태 손실)
- #3 (중간) licenses-content.tsx: 렌더 중 Date.now() 호출(불순)
- #4 (권한) dev-022 정책("권한 변경/시스템 설정은 SUPER_ADMIN 전용")이 가드에 미반영

## 변경
- #1: daysInput useState 를 조기 반환 위 useState 블록으로 이동
- #2: key fallback 을 Math.random() → map index(`row-${i}`)
- #3: 마운트 시 1회 고정 `const [now] = useState(() => Date.now())` 사용
- #4: 사용자 권한(role) 변경·ADMIN 생성·시스템 설정(gdrive-config/feature-flags) 변경을
      SUPER_ADMIN 전용으로 가드. 권한 계층 무결성을 위해 비-SUPER_ADMIN 은 SUPER_ADMIN
      계정을 강등/삭제/비활성화 불가. 서버 액션(admin/actions.ts)과 API 라우트 양쪽 적용.
  - 일상 운영(읽기/비밀번호 재설정/일반 USER 생성/활성화 토글)은 ADMIN 유지 → 잠금 위험 최소화.

## 미적용(후속)
- Phase 3 UI: 사용자 관리 폼/사이드바에 SUPER_ADMIN 토글·메뉴 분기, role 토글 버튼 비-super 비활성화
- #5(별건): cron-auth 상수시간 비교(crypto.timingSafeEqual)
- 운영: SUPER_ADMIN 0명 방지 — 마이그레이션 적용 시 활성 ADMIN 존재 확인
