TICKET_ID: dev-043
TITLE: lint 베이스라인 복구 — 전체 lint 에러 12건 해소(게이트 red→green)
SCOPE:
  - lib/i18n/index.tsx, app/reports/page.tsx, app/reports/[yearMonth]/report-detail-client.tsx (set-state-in-effect)
  - app/api/admin/archives/route.ts, app/licenses/[id]/page.tsx, app/licenses/page.tsx, licenses-content.tsx (no-explicit-any)
  - app/licenses/[id]/license-detail-content.tsx + page.tsx (no-children-prop)
ACCEPTANCE:
  - npm run lint exit 0 (에러 12 → 0). 경고 86건은 점진 정리(범위 밖)
  - npm run build 통과, 동작 불변
COMPLEXITY: medium
DEPLOY_FLAG: false
SECURITY_FLAG: false

## 배경 (후보군 로드맵 A1 — 메타 게이트)
CLAUDE.md 파이프라인의 'lint 통과' dev 완료 조건이 전체 기준 exit 1(에러 12건)로 이미 red.
게이트가 깨진 상태에선 후속 작업의 '회귀 없음' 판정이 불가 — 모든 작업의 메타-선행.
(그동안 PR은 변경 파일 기준 에러 0을 유지했으나 전체 베이스라인은 red였음)

## 수정 (에러 12건, 동작 불변)
- set-state-in-effect 4건(i18n locale 복원·비동기 로드, reports localStorage 프리셋): 전부
  SSR상 effect 필수인 정당한 초기화 패턴 → 이유 명시 eslint-disable(동작 0 변경).
- no-explicit-any 7건: (prisma as any)→prisma, (license as any).renewalStatus 등→license.X
  (dev-028 스칼라 필드라 캐스팅 불요), serializedLicenses as any→as unknown as EnrichedLicense[]
  (EnrichedLicense export 추가).
- no-children-prop 1건: LicenseDetailContent 의 children prop(ChildLicense[] 데이터, React 예약어 충돌)
  → childLicenses 리네임(컴포넌트 내부 사용처 + page 전달 일괄).

## 후속 (별도)
- 경고 86건(no-unused-vars 44·exhaustive-deps 25·no-unused-expressions 15) 점진 정리
- B1: next.config ignoreBuildErrors 끄기(tsc 잔존 에러 해소 후) / A2: 단위 테스트 도입

## 검증
- npm run lint exit 0(에러 0/경고 86), npm run build 통과.
