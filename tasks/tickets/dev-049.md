TICKET_ID: dev-049
TITLE: 결점 체크 후속 — 자산지도 데이터소실 P0·엣지 방향렌더·asset-links External 정합·i18n 네임스페이스 오참조
SCOPE:
  - app/asset-map/asset-map-content.tsx  (#1 위치소실 가드, #2 뷰전환 flush, #3 신규엣지 방향, #14 onReconnect)
  - app/api/asset-links/[id]/route.ts     (#13 PUT External 지원)
  - app/_components/pii-stage-select.tsx  (i18n t.asset→t.assetMap)
  - app/cloud/page.tsx                     (i18n renewalDate)
  - app/licenses/new/page.tsx              (i18n renewalDate)
ACCEPTANCE:
  - 비-all 뷰에서 노드 드래그/이동/뷰전환 시 all 뷰의 미표시 자산 위치·섹션이 보존된다 (영구소실 차단)
  - 신규 BI/REVERSE/CONDITIONAL 엣지가 생성 직후에도 재조회 후와 동일하게 렌더된다
  - 외부엔티티가 걸린 연결을 PUT 으로 수정할 수 있고, 외부 노드로 재연결해도 무음 404 가 나지 않는다
  - tsc 앱코드 에러 0 (ignoreBuildErrors 가 가렸던 8건 해소), PII 단계 라벨·갱신예정일이 런타임에 정상 표기
COMPLEXITY: high
DEPLOY_FLAG: false   (스키마 불변 — 기존 컬럼 활용. asset-links/[id] PUT 로직 변경뿐)
SECURITY_FLAG: false

---

## 배경 — 결점 체크(멀티에이전트 감사) 결과

dev-039~048 누적 변경을 4갈래(자산지도 정합성·PII 모델·API 일관성·아키텍처)로 점검 후 적대적 검증.
14건 진성 확정(false positive 0). 본 티켓은 즉시조치 대상(P0/P1 버그)을 담는다.

### 조치한 결함

- **#1 (P0)** 비-all 뷰 자동저장이 nodePositions/sectionData 를 부분집합으로 전체 덮어써 all 뷰 미표시 자산 위치가 영구 소실.
  dev-048 이 edgeVisibility 만 all-뷰 가드하고 위치/섹션을 빠뜨린 비대칭. → flushSave·beforeunload 에서 세 값 모두 all 뷰에서만 본문 포함(비-all 은 생략 → PUT 의 undefined 스킵으로 서버값 보존), savedState 도 동일 가드.
- **#2 (P1)** 뷰 전환(setView) 시 보류 자동저장 미flush·타이머 미취소 → 전환 후 부분집합 저장. → changeView 에서 전환 전 flushSave + 타이머 clear.
- **#3 (P1)** handleSaveLink 의 newEdge 가 direction 분기 없이 markerEnd 만 설정 → 신규 BI/REVERSE/CONDITIONAL 이 재조회 전까지 오렌더, 패널 isBi 오판정. → fetchGraph 와 동일하게 BI markerStart·REVERSE swap·CONDITIONAL dash/opacity 적용 + data.condition 보강.
- **#13 (P1)** asset-links PUT 가 External 연결 수정 미지원(include 누락·자기연결 검사 누락). → 4종 대칭 처리 + 상호배타 + include 보강 + Number.isInteger 가드.
- **#14 (P1)** onReconnect 가 target 만·Number("ext-5")=NaN 으로 무음 404 + UI/DB desync. → 양 엔드포인트·ext- 분기·REVERSE 역매핑·res.ok 실패 롤백.
- **i18n (P1, arch)** ignoreBuildErrors:true 가 가린 tsc 8건. 원인은 키 부재가 아니라 네임스페이스 오참조:
  pii-stage-select 가 t.asset.pii*(키는 assetMap 에 존재), cloud/page·licenses/new 가 t.license.renewalDate(키는 cloud 에 존재). → 참조를 실제 키 위치로 교정(단일 출처 유지, 키 복제 안 함).

### 검증
- tsc --noEmit 앱코드 0 errors (생성파일 validator.ts 제외)
- npm run lint 0 errors / 86 warnings (베이스라인 동일, 신규 경고 0)
- npm run build ✓ 95/95

### 후속(별도 트랙)
- 기능완결 P1: #7 PiiItemsEditor lifecycleStages/destructionMethod 입력, #8 LinkModal piiItems 카탈로그 통합, #9 ISMS-P 흐름표 export → dev-050+
- 부채 P2/P3: tasks/defect-backlog-2026-06.md 참조
