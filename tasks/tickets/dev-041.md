TICKET_ID: dev-041
TITLE: 네트워크 구성도 1단계 — 자산 노드에 IP/VLAN/hostname 배지 (N1)
SCOPE:
  - app/api/asset-map/route.ts — assetSelect 2곳에 네트워크 필드 추가 + 응답 매핑
  - app/asset-map/asset-map-content.tsx — AssetNode 타입·node.data·AssetNodeComponent 배지(network 뷰 전용)
ACCEPTANCE:
  - network 뷰에서 자산 노드에 IP 주소·VLAN·hostname 배지가 표시된다
  - all/pii/data_flow 뷰 외형은 불변 (배지는 mapView==="network"일 때만)
  - 작은 노드(isCompact)에서는 배지 숨김 (기존 progressive hiding 준수)
  - npm run build / lint 통과(기준선 동일)
COMPLEXITY: low
DEPLOY_FLAG: true   # API select·응답 동작 변경
SECURITY_FLAG: false

## 배경 (설계 문서 asset-map-viz-upgrade.md — 권장 1순위 N1)
network 뷰는 HardwareDetail의 네트워크 필드(ipAddress/subnetMask/gateway/vlanId/hostname,
schema 564-610)가 시각화에 0% 활용. API select가 deviceType만 가져옴(route.ts:83,103).
검증 결과 "순수 additive, 레이아웃·저장·복원 미변경 → 회귀 위험 0"으로 1순위 추천.

## 구현
- route.ts: assetSelect 2곳(all/필터 뷰)에 ipAddress/vlanId/gateway/subnetMask/hostname 추가,
  nodes.map 응답에 전파.
- asset-map-content.tsx: AssetNode 타입 + node.data 에 5개 필드 + mapView(현재 뷰) 주입.
  AssetNodeComponent 가 mapView==="network" && !isCompact 일 때만 IP/VLAN/hostname 배지 렌더.
- 레이아웃(getPiiLifecycleLayout/getLayoutedElements)·flushSave·복원 effect 한 줄도 미변경 → 회귀 0.

## 다음 (설계 로드맵)
- P1: PII 흐름선 암호화 분류(protocol 파싱) — 별도 PR
- P0: 저장/복원 양측 가드 — 2D 매트릭스/5계층 레이아웃의 선행 필수

## 검증
- npm run build 통과(94/94), lint master 기준선과 동일(신규 0).
