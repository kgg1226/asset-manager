TICKET_ID: dev-038
TITLE: 탭 구성 정리 — 관리 그룹을 "운영"과 "분류·기준"으로 분리
SCOPE:
  - app/sidebar.tsx
  - lib/i18n/* — nav.taxonomy 키 (7파일)
ACCEPTANCE:
  - 관리 11개 항목이 운영(7: 사용자/기기/증적/환율/플래그/가져오기/가이드)과
    분류·기준(4: 자산분류/증적 카테고리/직책별 CIA/라이선스 그룹)으로 분리 표시
  - 항목 삭제·경로 변경 없음 (증적 카테고리는 D2 보류 결정대로 유지)
  - npm run build / lint 통과
COMPLEXITY: low
DEPLOY_FLAG: false
SECURITY_FLAG: false

## 배경 (백로그 F6)
5그룹 25항목 중 admin 그룹에 11개 집중 — 일상 운영 기능과 "기준 정의" 기능이 섞여 탐색이 어려움.
그룹 분리만으로 가독성 확보(접기 기능은 기존 collapsible 재사용). 양쪽 모두 adminOnly 유지.

## 검증
- npm run build 통과, lint 신규 이슈 0(경고 2건은 기존 미사용 import).
