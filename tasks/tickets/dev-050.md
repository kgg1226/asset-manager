TICKET_ID: dev-050
TITLE: PII 증적 봉합(입력·출력) — PiiItemsEditor 단계·파기방법 입력 + ISMS-P 개인정보 흐름표 export
SCOPE:
  lib/pii-stage.ts                          (PII_STAGE_LABEL 단일출처 가산)
  app/_components/pii-items-editor.tsx      (PiiItemRow 확장 + parsePiiItemRows + 단계칩/파기/비고 UI)
  app/hardware/[id]/edit/page.tsx           (edit-load → parsePiiItemRows)
  app/domains/[id]/edit/page.tsx            (동일)
  app/cloud/[id]/edit/page.tsx              (동일)
  app/contracts/[id]/edit/page.tsx          (동일)
  app/api/reports/pii-flow/route.ts         (신규 export 엔드포인트)
  app/reports/page.tsx                      (흐름표 다운로드 버튼 — 출력단 도달경로)
ACCEPTANCE:
  - 자산 edit 폼에서 저장된 lifecycleStages 칩·destructionMethod·note 가 복원되어 보인다(4개 유형)
  - 단계 다중선택+파기방법 입력 후 저장→재진입 시 값 유지(round-trip)
  - GET /api/reports/pii-flow (ADMIN)이 노드(보유)+엣지(이동) 행 포함 xlsx 다운로드; 비ADMIN 403·미인증 401
COMPLEXITY: medium
DEPLOY_FLAG: true   (신규 API 라우트)
SECURITY_FLAG: true (관리자 PII 데이터 export — 인증 가드 필수)

---

## 배경 — 결점 체크 #9 "출력단 부재" + #7 "입력단 결손" 봉합

PII 증적 파이프라인의 입력단(lifecycleStages/destructionMethod 위젯 부재→영구 NULL)과
출력단(ISMS-P 흐름표 export 0건)을 함께 닫는다. 스키마는 컬럼 기존재 → 무변경.

### #7 입력단
- PiiItemRow 에 lifecycleStages(string[])·destructionMethod·note 추가(옵셔널 → 기존 import 무파손).
- 에디터에 단계 다중칩(PII_STAGES 재사용)·파기방법·비고 입력 추가.
- edit-load 4폼이 동일 인라인 map 으로 3필드만 복원하던 것을 parsePiiItemRows() 단일출처로 통일
  (lifecycleStages 는 DB JSON 문자열 → 배열 복원). 서버 POST/PUT 는 이미 4필드 수용 → 무변경.
- new 폼 4개: 에디터 공유 state 라 무변경(빈 배열 초기값 유지).

### #9 출력단
- /api/reports/pii-flow GET — api/export/all 컨벤션(getCurrentUser+ADMIN, ExcelJS, FF2563EB 헤더).
- 노드 행(자산 보유 AssetPiiItem) + 엣지 행(AssetLink.piiItems 이동) 결합, 6컬럼+구분/자산/비고.
- 엣지 항목은 isPiiItemKey 로 코드→라벨, 레거시 자유텍스트는 원문 유지(하위호환, dev-051 통합 전 안전).
- reports 페이지에 다운로드 버튼 추가(asset-register 옆) — 출력단 도달경로 확보.

### 단일출처
- 단계 라벨이 에디터·export·i18n 3곳으로 흩어지는 것을 막아 lib/pii-stage.ts 에 PII_STAGE_LABEL 가산.

### 검증
- tsc 앱코드 0 errors · lint 0 errors/86 warnings(신규 0) · build 96/96(신규 라우트 포함)
- security: 신규 라우트 getCurrentUser+ADMIN 가드 확인, 하드코딩 시크릿 0

### SCOPE 비고
권고 상한 5 초과(8). 단 4개 edit 폼은 동일 1줄 변경, lib/pii-stage·reports/page 는 소폭.
new 폼 4개는 무변경으로 SCOPE 제외. 출력단 도달경로(reports 버튼) 포함을 위해 확장.

### 후속
- #8 어휘통합(LinkModal 카탈로그 칩) → dev-051
- PII 기능 다국어(에디터·흐름표 버튼 라벨 한국어 하드코딩) → 백로그
