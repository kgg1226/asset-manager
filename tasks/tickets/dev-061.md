TICKET_ID: dev-061
TITLE: 연결선 깜빡임 드랍(핸들 타이밍) + 팔레트 제거 부활(병합저장 명시삭제 누락) 수정
SCOPE:
  app/asset-map/asset-map-content.tsx  (fetchGraph/handleSaveLink 2단 커밋·onRemoveFromCanvas 명시 제거)
  package.json                          (1.61.0)
  tasks/lessons.md
ACCEPTANCE:
  - 네트워크 뷰 포함 모든 뷰에서 연결선이 뷰 진입/재조회 때마다 안정적으로 렌더(깜빡임·유실 없음)
  - 신규 연결 생성 직후에도 선이 즉시·안정 표시
  - 팔레트 ✕/전체 제거로 캔버스에서 뺀 자산이 재조회 후 부활하지 않음(연결 소속은 유지 → 재배치 시 복귀)
COMPLEXITY: medium
DEPLOY_FLAG: false
SECURITY_FLAG: false

---

## 배경 — 사용자 화면 녹화(2026-07-06, v1.60.0) 프레임 분석
네트워크 뷰에서 연결선이 프레임에 따라 보였다 안 보였다(f05✗ f06✓ f07✗ f08✓ f09✗) —
사이드패널 연결 수(React 상태)는 유지 → 필터가 아니라 **렌더 단계 드랍**.

## 원인 2건
① **동적 핸들 타이밍(기존 교훈 미적용 경로)**: fetchGraph·handleSaveLink 가 setNodes(핸들 주입)와
   setEdges 를 같은 커밋에서 실행 → -pN 핸들이 DOM 등록되기 전에 엣지가 참조 → React Flow 가
   해당 선을 조용히 드랍(타이밍 따라 깜빡임, 병렬 엣지 많은 네트워크 뷰에서 두드러짐).
   lessons.md 2026-03-23 처방(setNodes→rAF→setEdges)이 이 두 경로엔 빠져 있었다.
② **팔레트 제거 부활**: onRemoveFromCanvas 가 로컬 상태만 제거 — 병합 저장(dev-059)은
   "키 부재=삭제"를 표현하지 못하므로 저장본에 위치가 남아 재조회 시 자산이 부활.

## 수정
- fetchGraph(pii/기본 분기)·handleSaveLink: setNodes 먼저 → requestAnimationFrame 후 setEdges.
  handleSaveLink 는 setEdges 업데이터 내부 setNodes(안티패턴)도 해소(reactFlowInstance.getEdges() 기반).
- onRemoveFromCanvas: placedAssetIdsRef + 저장본 nodePositions(ref+state, 문자열 JSON 호환)에서
  해당 키 명시 제거 → 병합 저장이 삭제를 영속화. 연결 소속은 유지(재배치 시 연결 복귀 — dev-059 의도).

## 검증
- tsc 0 · lint 0 errors/86 warnings · build 98/98
