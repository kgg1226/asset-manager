TICKET_ID: dev-066
TITLE: PII 흐름도 주체 레인(스윔레인) — 단계×주체 2차원 격자 + 정보주체 노드
SCOPE:
  app/asset-map/asset-map-content.tsx    (레인 레이아웃·PiiLaneLabel 노드타입·제외처리)
  app/api/external-entities/route.ts     (DATA_SUBJECT 유형)
  app/api/external-entities/[id]/route.ts
  app/external/new/page.tsx · app/external/page.tsx (유형 선택·배지·필터)
  lib/i18n/** (types + 6로케일)           (4키)
  package.json (1.66.0)
ACCEPTANCE:
  - PII 뷰가 행(수집/저장/이용·제공/파기) × 열(정보주체/내부 시스템/수탁·제3자) 격자로 배치된다
  - 외부조직에 "정보주체" 유형을 만들 수 있고, 그 노드가 정보주체 열에 배치돼 수집 흐름의 출발점이 된다
  - 레인 헤더는 저장·선택·레이아웃 대상에서 제외(위치 오염 없음), 다른 뷰는 영향 없음
COMPLEXITY: medium
DEPLOY_FLAG: false  (API 는 문자열 화이트리스트 확장 — 스키마 무변경)
SECURITY_FLAG: false

---

## 배경 — 목적 재정렬 (사용자 지적)
자산지도의 원래 목적은 ① 시스템 구조도 ② 개인정보 흐름도 산출이었는데, dev-054~065 를
조작 UX·버그에만 썼다. 산출물 문법(레인·순번·범례)에는 티켓이 0개였다.

진단: 두 산출물 모두 "재료(자산·연결·증적 필드)는 충분한데 문법이 없다". 표준 ISMS-P
흐름도가 읽히는 이유는 행(생명주기 단계)×열(처리 주체) 격자인데, 기존 PII 뷰는 행만 있어
점과 선의 뭉치로 남았다. 레인 하나가 두 산출물의 공통 병목.

## 구현 (개인정보 흐름도 프리셋 우선 — 사용자 선택)
- getPiiLifecycleLayout 에 주체 레인 3열 도입: 정보주체 / 내부 시스템 / 수탁·제3자.
  laneOf(): 외부조직 DATA_SUBJECT→0, 그 외 외부조직→2, 자산→1.
  셀([단계][레인])별 노드 배분 → 레인 폭 = 그 레인의 최대 셀 밀도 → 누적 X 좌표.
- piiLaneLabel 노드 타입 신규(점선 테두리 헤더, 레인별 색). 저장·선택·dagre·클릭 경로에서
  piiStageLabel 과 동일하게 제외 처리(위치 저장 오염 방지).
- 외부조직 유형에 DATA_SUBJECT 추가(String 컬럼이라 스키마 무변경) — 흐름도의 수집 출발점.
- i18n 4키(typeDataSubject + piiLaneSubject/Internal/External) × 6로케일.

## 후속 (dev-067+)
- 흐름 서사: 엣지 순번(①②③)·암호화 흐름 강조·범례
- 시스템 구조도 프리셋: 영역/계층 레인(사용자·DMZ·앱·데이터·외부연동)
- 흐름도 도식 PDF 산출

## 검증
- tsc 0 · lint 0 errors/86 warnings · build 98/98
