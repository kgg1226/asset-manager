TICKET_ID: dev-065
TITLE: 하드웨어 장비 유형군별 조건부 필드 표시 — 데이터 은닉 없는 정리
SCOPE:
  lib/hardware-device-types.ts          (신규 — DEVICE_TYPES 단일출처 + 유형군 매트릭스)
  app/hardware/new/page.tsx             (조건 렌더)
  app/hardware/[id]/edit/page.tsx       (조건 렌더 + 저장값 있으면 계속 표시)
  app/hardware/[id]/page.tsx            (기기관리 패널 비해당 군 접힘)
  lib/i18n/** (types + 6로케일)          (deviceMgmtCollapsed 1키)
  package.json                           (1.65.0)
ACCEPTANCE:
  - Network/SecurityDevice 자산의 new/edit 에서 OS·OS버전·PII 단계·PII 항목이 노출되지 않음
    (펌웨어 버전은 기존 인프라 블록 필드가 정본), Monitor 등 주변기기는 호스트명·MAC/IP·PII 도 비노출
  - edit 에서 숨김 대상 필드에 기존 저장값이 있으면 계속 표시(은닉·데이터 손실 0), payload 전 필드 유지
  - 미지정/Other/목록 외 자유값(CSV 유입)은 전체 표시(안전 폴백), deviceType 저장 값 문자열 14종 불변
COMPLEXITY: high
DEPLOY_FLAG: false
SECURITY_FLAG: false

---

## 배경 (사용자 요구)
"장비 유형에 따라 넣는 값이 장비 분류에 맞게. 네트워크 장비에 OS 버전은 왜 있고 개인정보 처리
단계는 왜 표시되냐. 기기관리 같은 것도." — 삭제 금지 원칙은 기능/데이터 보존 원칙이며 조건 표시와
무관함을 확인, 조건부 표시로 정리.

## 유형군 × 필드 매트릭스 (설계 워크플로우 확정안)
- COMPUTING(Laptop/Desktop/Server): 전체
- NETWORK(Network/SecurityDevice): OS·PII·스펙·기기관리 X — 펌웨어 버전(기존 인프라 블록)이 정본
- MOBILE(Mobile): OS·PII·기기관리 O, IP/호스트명 X
- STORAGE(Storage/Backup): PII O(보유/파기 해당), OS·기기관리 X
- PERIPHERAL(Monitor/Peripheral/Rack/Component/Facility): 네트워크 식별·OS·PII·기기관리 X
- FALLBACK(미지정/Other/자유값): 전체 표시

## 구현
- lib/hardware-device-types.ts: DEVICE_TYPES 14종 승격(중복 상수 제거 — new:20/edit:20),
  resolveDeviceGroup(대소문자 하위호환)·isFieldVisible. "저장 값 문자열 변경 금지" 주석
  (자산맵 Network 하드코딩·PC 감가상각·리포트 분류가 원시 문자열 참조).
- new: 조건 렌더만. edit: `표시 = 유형 허용 ∨ 저장값 존재` — 숨김이 은닉이 되지 않고,
  payload 전 필드 유지로 저장 데이터 불변. 유형별 서브폼(기존)·인프라 블록 조건은 불변.
- 상세: 표시 행은 값 존재 기반이라 추가 게이팅 불요(은닉 위험 회피). 기기관리 패널만
  비해당 군에서 <details> 접힘(기능·핸들러 보존 — 이미 managed 지정 장비 접근 유지).

## 후속(roadmap 등재)
- deviceType 서버측 whitelist 검증(API·CSV 임포트), asset-register 리포트의 ServerBlade/NAS/SAN
  불일치 정리, 목록/상세 deviceType 셀 i18n 출력.

## 검증
- tsc 0 · lint 0 errors/86 warnings · build 98/98
